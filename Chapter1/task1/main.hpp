#pragma once
# include <iostream>
# include <queue>

unsigned long				mc;
unsigned long				clk[4];
char						*r;
std::queue<unsigned long>	broken_machines;

# define NaN (unsigned long)-1
# define MAX (unsigned long)-1
